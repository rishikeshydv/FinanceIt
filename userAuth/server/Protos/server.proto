syntax = "proto3";

option csharp_namespace = "authService";

package auth;

service authCheck {
  rpc authRequest (authRequest) returns (authReply);
}

message authRequest {
  string userid = 1;
  int32 pass = 2;
}

message authReply {
  bool isAccepted = 1;
}
